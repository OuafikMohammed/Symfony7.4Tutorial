<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Posts Management{% endblock %}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        
        <!-- Tailwind CSS CDN -->
        <script src="https://cdn.tailwindcss.com"></script>
        
        <!-- Google Fonts -->
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet">
        
        <!-- Favicon -->
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        
        <style>
            :root {
                --font-sans: 'Inter', sans-serif;
                --font-display: 'Poppins', sans-serif;
            }
            
            body {
                font-family: var(--font-sans);
                @apply bg-gradient-to-br from-slate-50 via-blue-50 to-purple-50;
            }
            
            h1, h2, h3, h4, h5, h6 {
                font-family: var(--font-display);
                @apply font-bold tracking-tight;
            }
            
            h1 { @apply text-4xl md:text-5xl; }
            h2 { @apply text-3xl md:text-4xl; }
            h3 { @apply text-2xl; }
            
            .btn {
                @apply px-4 py-2 rounded-lg font-medium transition-all duration-200 inline-block;
            }
            
            .btn-primary {
                @apply bg-blue-600 text-white hover:bg-blue-700 shadow-md hover:shadow-lg hover:scale-105;
            }
            
            .btn-success {
                @apply bg-green-600 text-white hover:bg-green-700 shadow-md hover:shadow-lg hover:scale-105;
            }
            
            .btn-danger {
                @apply bg-red-600 text-white hover:bg-red-700 shadow-md hover:shadow-lg;
            }
            
            .btn-secondary {
                @apply bg-slate-500 text-white hover:bg-slate-600;
            }
            
            .btn-info {
                @apply bg-cyan-600 text-white hover:bg-cyan-700;
            }
            
            .form-input {
                @apply w-full px-4 py-2 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all;
            }
            
            .form-label {
                @apply block text-sm font-semibold text-slate-700 mb-2;
            }
            
            .form-group {
                @apply mb-4;
            }
            
            .card {
                @apply bg-white rounded-xl shadow-sm hover:shadow-md transition-shadow border border-slate-200;
            }
            
            .badge {
                @apply inline-block px-3 py-1 rounded-full text-sm font-medium;
            }
            
            .badge-blue {
                @apply bg-blue-100 text-blue-800;
            }
            
            .badge-green {
                @apply bg-green-100 text-green-800;
            }
            
            .alert {
                @apply p-4 rounded-lg border;
            }
            
            .alert-info {
                @apply bg-blue-50 border-blue-200 text-blue-800;
            }
            
            .alert-success {
                @apply bg-green-50 border-green-200 text-green-800;
            }
            
            .alert-danger {
                @apply bg-red-50 border-red-200 text-red-800;
            }
        </style>
        
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body class="min-h-screen">
        {% block content %}
            <div class="max-w-6xl mx-auto px-4 py-8">
                {% block body %}{% endblock %}
            </div>
        {% endblock %}
    </body>
</html>
