{% extends 'base.html.twig' %}

{% block title %}Blog - URL Generation Examples{% endblock %}

{% block body %}
    <div class="mb-8">
        <h1 class="text-blue-600 mb-2">🚀 URL Generation Patterns</h1>
        <p class="text-slate-600 text-lg">Examples of how to generate URLs in Symfony controllers</p>
    </div>

    <div class="grid gap-4 md:grid-cols-2">
        {% for post in posts %}
            <div class="card p-6 hover:shadow-lg transition-all duration-300 border-l-4 border-l-blue-500">
                <h2 class="text-lg font-semibold text-slate-800 mb-2">{{ post.title }}</h2>
                <p class="text-slate-600 line-clamp-3">{{ post.content }}</p>
                <a href="{{ path('post_show', {id: post.id}) }}" class="inline-block mt-4 text-blue-600 hover:text-blue-700 font-medium">Read more →</a>
            </div>
        {% endfor %}
    </div>
{% endblock %}